import RegisterForm from "../ui/register";

export default function Register() {

    return (
        <div className="flex items-center justify-center bg-gray-500/70 min-h-screen">
            <RegisterForm/>
        </div>
    );
}